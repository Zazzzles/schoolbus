import { Title, Description } from '@storybook/addon-docs/blocks'
import { Meta } from '@storybook/addon-docs/blocks'
import Flex from '../../components/flex'
import Box from '../../components/box'
import Text from '../../components/text'
import theme, { radiiAliases } from '../../config/theme'

<Meta title='Theme/ Radii' />

# Radii
A list of the available radii properties. 
Can be used in places where a component accepts a shadow,
or can be accessed through the theme.

<Flex flexDirection='column'>
  {radiiAliases.map(key => (
    <Flex
      key={key}
      justifyContent='space-between'
      alignItems='center'
      mb='2.5em' 
    >
      <Text fontWeight='extrabold'>
        theme.radii.{key}
      </Text>
      <Box
        alignItems='center'
        border='solid #FFA400 1px'
        radius={key}
        p='2em' 
        width='50%'
      >
        <Text textAlign='center'>{theme.radii[key]}</Text>
      </Box>
    </Flex>
  ))}
</Flex>